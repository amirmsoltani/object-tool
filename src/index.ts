import filter from './filter';
import map from './map';
import mapToArray from './mapToArray';
import etch from './etch';
import len from './len';
module.exports = {
    filter,
    map,
    mapToArray,
    etch,
    len
};